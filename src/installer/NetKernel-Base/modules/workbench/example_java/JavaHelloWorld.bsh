//Test Harness to execute Dynamic Java
import example_java.JavaHelloWorld;
import org.ten60.netkernel.layer1.representation.*;

main()
{	JavaHelloWorld jhw=new JavaHelloWorld();
	sa=new StringAspect(jhw.getMessage());
	resp=context.createResponseFrom(sa);
	resp.setMimeType("text/plain");
}