<config>
	<rdbms>
    	    <!-- hsql -->
            <!-- <jdbcDriver>org.hsqldb.jdbcDriver</jdbcDriver>
            <jdbcConnection>jdbc:hsqldb:purl</jdbcConnection>
            <user>sa</user>
            <password/>
            <shutdown>true</shutdown> -->

            <!-- mysql -->
            <jdbcDriver>com.mysql.jdbc.Driver</jdbcDriver>
            <jdbcConnection>jdbc:mysql://$MYSQLHOST:$MYSQLPORT/$MYSQLDBNAME</jdbcConnection>
            <user>$MYSQLUSERNAME</user>
            <password>$MYSQLPASSWORD</password>
            <poolSize>100</poolSize>
	</rdbms>
</config>